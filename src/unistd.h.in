#ifndef UJSON_UNISTD_H
#define UJSON_UNISTD_H

// Windows is missing unistd.h, so we only fill
// it in with the bare minium needed by the source
// code generated by BISON. And also ssize_t.

#ifdef _MSC_VER
#include <BaseTsd.h>
typedef SSIZE_T ssize_t;
#endif

#include <stdlib.h>
#include <process.h>
#include <direct.h>
#include <io.h>

#define fileno _fileno
#define isatty _isatty

#endif
