@PACKAGE_INIT@

include ("${CMAKE_CURRENT_LIST_DIR}/ujsonTargets.cmake")

get_target_property (UJSON_INCLUDE_DIRS ujson INTERFACE_INCLUDE_DIRECTORIES)
get_target_property (UJSON_LIBRARIES ujson INTERFACE_LINK_LIBRARIES)

check_required_components(ujson)
